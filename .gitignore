# Dependency directories
node_modules/
**/node_modules/

# Next.js Build Outputs & Caches
.next/
**/.next/
out/
**/out/
*.tsbuildinfo
next-env.d.ts

# Environment configuration files (Do NOT commit secrets)
.env
.env.local
.env.production
.env.development
.env.test
.env.local
.env.*.local
.env.production.local
**/.env
**/.env.local
**/.env.production
**/.env.*

# AI Agents / Scratch files / Local logs
.agents/
.claude/
scratch/
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Local datasets (source CSVs already imported into Supabase — do not commit)
dataset/

# Android TWA signing keys + build artifacts (NEVER commit the app-signing keystore —
# it is the root of trust for Play Store updates). Keep the keystore backed up out-of-band.
*.keystore
*.jks
android-twa/.gradle/
android-twa/app/build/
android-twa/build/

# Test coverage
coverage/
**/coverage/
.nyc_output/

# Build artifacts
dist/
**/dist/
build/
**/build/

# Operating System specific files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
[Dd]esktop.ini

# IDEs & Editor Configuration
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
