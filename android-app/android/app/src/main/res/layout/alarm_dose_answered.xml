<?xml version="1.0" encoding="utf-8"?>
<!--
  A dose that has just been answered, left on screen.

  IT STAYS RATHER THAN VANISHING. Answering one of four and watching the row
  disappear leaves you counting what is left to work out whether the tap
  registered; a row that turns into "✓ Taken — Metformin" answers that without
  being asked. It is also the only record the patient has that their tap landed,
  since the queue write is invisible and the server may be hours away.

  Not interactive: correcting a dose needs the history surface, which is a
  judgement about the past and belongs on the web, not on a 3am alarm screen.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:layout_marginTop="10dp"
    android:paddingStart="14dp"
    android:paddingEnd="14dp"
    android:paddingTop="16dp"
    android:paddingBottom="16dp"
    android:background="@drawable/bg_alarm_card_quiet">

    <TextView
        android:id="@+id/dose_verdict"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="17sp"
        android:textStyle="bold"
        android:textColor="@color/verdict_taken" />

    <TextView
        android:id="@+id/dose_name"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:textSize="17sp"
        android:textColor="@color/alarm_ink_muted" />
</LinearLayout>
